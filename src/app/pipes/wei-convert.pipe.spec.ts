import { WeiConvertPipe } from './wei-convert.pipe';

describe('WeiConvertPipe', () => {
  it('create an instance', () => {
    const pipe = new WeiConvertPipe();
    expect(pipe).toBeTruthy();
  });
});
